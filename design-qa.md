# 格物志 · 液态岛扩展设计 QA

## 视觉真源与实现证据

- 真源：`reference-claude-gewu-20260720/02-reference-control-center.png`
- 实现：`02-liquid-island-open.png`
- 同屏全页对照：`liquid-island-qa-comparison.png`
- 同屏重点对照：`liquid-island-qa-focused.png`
- 桌面视口与状态：1440 × 1024，首页首屏，液态控制中心展开。
- 移动视口与状态：390 × 844，首页首屏与液态控制中心展开；另检查专题长读和材料文章页。

## 对照结论

- 液态岛保持真源的黑色胶囊、金色状态点、圆形展开按钮和弹性出现方式；实现额外显示当前所处章节。
- 控制中心保持真源的居中悬浮、厚玻璃材质、顶部抓手、2 × 3 操作矩阵和全页背景去焦。实现根据本站信息架构替换为六个真实入口。
- 实现面板略宽于真源，用于容纳中文入口且保证手机端仍为两列；层级、间距、圆角、玻璃厚度和阴影关系一致。
- 焦点态、展开态与拖拽提示都可见；面板关闭时使用 `inert`，不会留下不可见的可聚焦控件。
- 手机端控制中心完整落在 390px 视口内，页面 `scrollWidth` 与 `clientWidth` 一致，无横向溢出。

## 丰富内容验证

- 材质图鉴完整呈现 `blur(8px)`、`blur(20px)`、`blur(34px)` 三个玻璃层级。
- 弹簧实验可调整阻尼比与响应时间，释放后实际运行阻尼振子并输出过冲量与稳定用时。
- 五篇专题长读全部可打开，拥有相邻文章导航、键盘左右切换和权威来源卡片。
- 五个主题页都保留原来的四分支结构，并新增进入对应专题长读的入口。

## 修正记录

1. P1 · 原站顶部普通白色长条覆盖了 Claude 版本的液态状态岛：重构为居中黑色液态岛，并补回厚玻璃控制中心与背景模糊。
2. P2 · 参考控制中心关闭时存在隐藏交互暴露风险：实现增加 `inert`、`aria-expanded`、动态标签、Escape/空白关闭和焦点恢复。
3. P2 · 初版弹簧滑杆缺少独立可访问名称：补充中英双语 `aria-label`。
4. P2 · 参考版本材质图鉴入口前留白偏长：实现压缩段落间距，并让图鉴、实验、长读形成连续的滚动节奏。
5. P2 · Claude 长读内容没有明确来源区：为五篇文章补充论文、官方文档或原始文本链接。

## 交互与技术验证

- 液态岛点击、背景点击、Escape、向上轻甩关闭均已实现；六个入口均指向真实页面或区段。
- 弹簧实验运行完成，状态返回“已稳定”，并输出 3.5px 过冲和 1.03s 稳定用时（默认参数）。
- 桌面和移动端浏览器日志均无 error/warn。
- `museum.js` 与 `assets/notes.js` 语法检查通过；静态内部链接检查通过；`git diff --check` 通过。

final result: passed
